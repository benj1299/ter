version: "3.2"

services:
  app:
    container_name: app
    build: ./app
    entrypoint: /bin/sh
    stdin_open: true
    tty: true
    volumes: 
      - "./app:/usr/src/app"
    ports:
      - "5000:5000"
    restart: always
